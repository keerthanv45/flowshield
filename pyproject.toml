[project]
name = "flowshield"
version = "0.1.0"
description = "Autonomous Payment Reliability & Revenue Protection Agent (Razorpay Buildathon 2026)"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.setuptools]
packages = ["backend", "ml"]
